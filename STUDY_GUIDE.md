# 📚 Шпаргалка для защиты проекта GitForum

> **Важно**: Этот документ поможет разобраться в технологиях, использованных в проекте, и подготовиться к вопросам на защите.

---

## 🎯 Архитектура проекта

### Общая структура
```
Frontend (React + Vite) ←→ Nginx ←→ Backend (Django) ←→ PostgreSQL
                              ↓
                          Redis + Celery (фоновые задачи)
```

**Что будут спрашивать:**
- Почему выбрана именно такая архитектура?
- Зачем нужен Nginx, если можно обращаться к бэкенду напрямую?
- Что такое декаплинг frontend/backend?

**Ответы:**
- **SPA + REST API архитектура**: Frontend и Backend разделены, общаются через HTTP API
- **Nginx** - reverse proxy: раздает статику быстрее, балансирует нагрузку, работает как единая точка входа
- **Docker Compose** - оркестрация всех сервисов (изоляция, единая сеть, легкий деплой)

**Источники:**
- 📖 [Django REST Framework docs](https://www.django-rest-framework.org/)
- 📖 [Nginx как reverse proxy](https://docs.nginx.com/nginx/admin-guide/web-server/reverse-proxy/)
- 📺 [Docker Compose для разработчиков](https://www.youtube.com/watch?v=DM65_JyGxCo)

---

## 🔐 BACKEND: Django + DRF

### 1. **Django ORM и Models**

**Ваш код:**
```python
# core/models.py
class Repositories(models.Model):
    owner = models.ForeignKey('Users', models.DO_NOTHING)
    org = models.ForeignKey(Organizations, models.DO_NOTHING)
    ...
```

**Что будут спрашивать:**
- Что такое ORM и зачем он нужен?
- Чем отличается `ForeignKey` от `ManyToManyField`?
- Что значит `managed = False`?
- Почему используется `models.DO_NOTHING`?

**Ответы:**
- **ORM** (Object-Relational Mapping) - работа с БД через объекты Python, а не SQL
- **ForeignKey** - связь "многие к одному" (у репозитория один владелец, у владельца много репозиториев)
- **managed = False** - Django НЕ управляет этой таблицей (не создает миграции), таблица создана вручную или внешним инструментом
- **DO_NOTHING** - Django не удаляет связанные объекты при удалении родительского (контроль на уровне БД)

**Источники:**
- 📖 [Django Models](https://docs.djangoproject.com/en/5.0/topics/db/models/)
- 📖 [ForeignKey on_delete options](https://docs.djangoproject.com/en/5.0/ref/models/fields/#django.db.models.ForeignKey.on_delete)
- 📺 [Django ORM за 30 минут](https://www.youtube.com/watch?v=rP0GbbGlBnI)

---

### 2. **Django REST Framework (DRF)**

**Ваш код:**
```python
# settings.py
REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': (
        'dj_rest_auth.jwt_auth.JWTCookieAuthentication',
        'rest_framework.authentication.SessionAuthentication',
    ),
    'DEFAULT_PAGINATION_CLASS': 'rest_framework.pagination.PageNumberPagination',
    'PAGE_SIZE': 10,
}
```

**Что будут спрашивать:**
- Что такое REST API?
- Чем отличаются ViewSet, APIView, generics?
- Зачем нужны Serializers?
- Что такое пагинация и зачем она?

**Ответы:**
- **REST** - архитектурный стиль API: использует HTTP методы (GET/POST/PUT/DELETE), работает с ресурсами
- **ViewSet** - набор view для CRUD (список, создание, обновление, удаление) одной модели
- **Serializers** - преобразуют объекты Django в JSON и обратно, валидируют данные
- **Пагинация** - разбиение больших списков на страницы (не грузить 10000 записей за раз)

**Источники:**
- 📖 [DRF Tutorial](https://www.django-rest-framework.org/tutorial/1-serialization/)
- 📺 [REST API концепции](https://www.youtube.com/watch?v=lsMQRaeKNDk)
- 📖 [Serializers guide](https://www.django-rest-framework.org/api-guide/serializers/)

---

### 3. **Аутентификация: JWT + OAuth2 (Allauth)**

**Ваш код:**
```python
INSTALLED_APPS = [
    'allauth.socialaccount.providers.google',
    'allauth.socialaccount.providers.github',
    'dj_rest_auth',
]

REST_USE_JWT = True
JWT_AUTH_COOKIE = 'gitforum-auth'
```

**Что будут спрашивать:**
- Что такое JWT?
- Чем JWT отличается от Token/Session auth?
- Что такое OAuth2 и зачем он нужен?
- Как работает вход через GitHub?

**Ответы:**
- **JWT** (JSON Web Token) - зашифрованный токен с данными пользователя, хранится на клиенте
  - **Плюсы**: stateless (не хранится на сервере), масштабируемость
  - **Минусы**: нельзя отозвать до истечения срока
- **OAuth2** - протокол авторизации через сторонние сервисы (GitHub, Google)
- **Allauth** - библиотека Django для social auth (готовые провайдеры)

**Процесс входа через GitHub:**
1. Пользователь нажимает "Login with GitHub"
2. Redirect на GitHub → пользователь дает разрешения
3. GitHub возвращает код → обмен на access token
4. Запрос данных пользователя (email, username)
5. Создание/обновление пользователя в БД
6. Генерация JWT токена

**Источники:**
- 📖 [JWT.io - введение в JWT](https://jwt.io/introduction)
- 📖 [OAuth2 простыми словами](https://auth0.com/intro-to-iam/what-is-oauth-2)
- 📺 [OAuth2 explained](https://www.youtube.com/watch?v=996OiexHze0)
- 📖 [Django Allauth docs](https://docs.allauth.org/en/latest/)

---

### 4. **Celery + Redis (Асинхронные задачи)**

**Ваш код:**
```python
# docker-compose.yml
worker:
  command: celery -A config worker -l info

# settings.py
CELERY_BROKER_URL = 'redis://redis:6379/0'
```

**Что будут спрашивать:**
- Зачем нужен Celery?
- Что такое message broker и почему Redis?
- Какие задачи выполняются асинхронно?

**Ответы:**
- **Celery** - система распределенных задач (выполняет тяжелые операции в фоне)
- **Redis** - message broker (очередь задач) и результаты выполнения
- **Примеры задач:** парсинг репозиториев Git, отправка email, генерация отчетов

**Когда использовать:**
- Долгие операции (не блокировать HTTP запрос)
- Периодические задачи (cron-like)
- Обработка большого объема данных

**Источники:**
- 📖 [Celery docs](https://docs.celeryq.dev/en/stable/getting-started/introduction.html)
- 📺 [Celery за 10 минут](https://www.youtube.com/watch?v=THxCy-6EnQM)
- 📖 [Redis для начинающих](https://redis.io/docs/about/)

---

### 5. **PostgreSQL (БД)**

**Ваш код:**
```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'HOST': '34.41.176.77',  # облачная БД
    }
}
```

**Что будут спрашивать:**
- Почему PostgreSQL, а не MySQL/SQLite?
- Что такое индексы и зачем они?
- Что такое транзакции?

**Ответы:**
- **PostgreSQL плюсы:**
  - Полная поддержка SQL стандартов
  - JSON поля (JSONB) для гибких данных
  - Мощные индексы (GIN, GiST)
  - ACID транзакции
- **Индексы** - ускоряют поиск (аналог содержания в книге)
- **Транзакции** - набор операций "все или ничего" (ACID: Atomicity, Consistency, Isolation, Durability)

**Источники:**
- 📖 [PostgreSQL tutorial](https://www.postgresqltutorial.com/)
- 📺 [PostgreSQL за 100 секунд](https://www.youtube.com/watch?v=n2Fluyr3lbc)
- 📖 [Индексы в PostgreSQL](https://www.postgresql.org/docs/current/indexes.html)

---

### 6. **CORS и безопасность**

**Ваш код:**
```python
INSTALLED_APPS = ['corsheaders']
CORS_ALLOW_ALL_ORIGINS = True  # ⚠️ только для dev!
```

**Что будут спрашивать:**
- Что такое CORS?
- Почему браузер блокирует запросы с другого домена?
- Как это работает в production?

**Ответы:**
- **CORS** (Cross-Origin Resource Sharing) - механизм безопасности браузера
- Запрещает JavaScript с `site-a.com` делать запросы на `site-b.com` без разрешения
- В dev: frontend (`localhost:3000`) ≠ backend (`localhost:8000`) → нужен CORS
- В production: настроить `CORS_ALLOWED_ORIGINS = ['https://example.com']`

**Источники:**
- 📖 [CORS explained](https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS)
- 📺 [What is CORS?](https://www.youtube.com/watch?v=4KHiSt0oLJ0)

---

## ⚛️ FRONTEND: React + Vite

### 1. **React 19**

**Ваш код:**
```json
"react": "^19.2.0"
```

**Что будут спрашивать:**
- Что такое компоненты и почему React?
- Что такое Virtual DOM?
- Что такое hooks (useState, useEffect)?

**Ответы:**
- **React** - библиотека для UI (компонентный подход, переиспользование кода)
- **Virtual DOM** - копия DOM в памяти, React сравнивает и обновляет только изменения
- **Hooks** - функции для работы с состоянием и lifecycle:
  - `useState` - локальное состояние компонента
  - `useEffect` - побочные эффекты (API запросы, подписки)

**Источники:**
- 📖 [React docs](https://react.dev/)
- 📺 [React за 100 секунд](https://www.youtube.com/watch?v=Tn6-PIqc4UM)
- 📖 [React hooks](https://react.dev/reference/react)

---

### 2. **Vite (Build tool)**

**Что будут спрашивать:**
- Почему Vite, а не Create React App?
- Что такое HMR?

**Ответы:**
- **Vite** - современный bundler (быстрее Webpack/CRA)
  - Использует ES modules в dev (нет bundling)
  - esbuild для production (очень быстрый)
- **HMR** (Hot Module Replacement) - обновление кода без перезагрузки страницы

**Источники:**
- 📖 [Vite guide](https://vite.dev/guide/)
- 📺 [Vite vs Webpack](https://www.youtube.com/watch?v=KCrXgy8qtjM)

---

### 3. **Tailwind CSS 4**

**Ваш код:**
```json
"tailwindcss": "^4.1.17"
```

**Что будут спрашивать:**
- Что такое utility-first CSS?
- Плюсы/минусы Tailwind?

**Ответы:**
- **Utility-first** - маленькие классы для каждого CSS свойства (`flex`, `text-center`)
- **Плюсы:** быстрая разработка, консистентность, tree-shaking
- **Минусы:** "грязный" HTML, кривая обучения

**Источники:**
- 📖 [Tailwind docs](https://tailwindcss.com/docs)
- 📺 [Tailwind за 100 секунд](https://www.youtube.com/watch?v=mr15Xzb1Ook)

---

### 4. **Radix UI**

**Ваш код:**
```json
"@radix-ui/react-dialog": "^1.1.6"
```

**Что будут спрашивать:**
- Зачем библиотека компонентов?
- Почему Radix, а не Material UI?

**Ответы:**
- **Radix UI** - headless UI компоненты (только логика, стили свои)
- **Плюсы:** доступность (a11y), гибкость стилизации, легковесность
- Альтернативы: shadcn/ui (надстройка над Radix), HeadlessUI

**Источники:**
- 📖 [Radix UI](https://www.radix-ui.com/)
- 📖 [shadcn/ui](https://ui.shadcn.com/)

---

## 🐳 DevOps: Docker + Docker Compose

### Docker

**Ваш файл:**
```dockerfile
# backend/Dockerfile
FROM python:3.11-slim
COPY requirements.txt .
RUN pip install -r requirements.txt
```

**Что будут спрашивать:**
- Что такое Docker и контейнеризация?
- Чем отличается образ от контейнера?
- Что такое Docker volumes?

**Ответы:**
- **Docker** - платформа для запуска приложений в изолированных контейнерах
- **Образ (image)** - шаблон с кодом и зависимостями (read-only)
- **Контейнер** - запущенный экземпляр образа
- **Volumes** - сохранение данных вне контейнера (в вашем случае: `node_modules`, статика)

**Источники:**
- 📖 [Docker overview](https://docs.docker.com/get-started/overview/)
- 📺 [Docker за 100 секунд](https://www.youtube.com/watch?v=Gjnup-PuquQ)
- 📺 [Docker Compose tutorial](https://www.youtube.com/watch?v=DM65_JyGxCo)

---

## 🔥 Типичные вопросы на защите

### О проекте в целом

**1. Опишите архитектуру проекта**
```
Frontend (React SPA) 
    ↓ HTTP requests
Nginx (reverse proxy)
    ↓
Django REST API
    ↓
PostgreSQL (data) + Redis (cache/queue)
    ↓
Celery worker (background tasks)
```

**2. Как происходит аутентификация пользователя?**
- Social auth (GitHub/Google) через OAuth2
- Генерация JWT токена
- Токен хранится в cookie (`gitforum-auth`)
- Frontend посылает токен в каждом API запросе

**3. Что делать, если много пользователей одновременно?**
- Горизонтальное масштабирование (больше контейнеров Django)
- Redis для кэширования
- PostgreSQL индексы для быстрых запросов
- Celery для фоновых задач (не блокировать основной поток)

---

### Технические вопросы

**4. Почему используется Docker Compose, а не Kubernetes?**
- Docker Compose проще для dev и малых/средних приложений
- Kubernetes нужен для огромных систем с автомасштабированием
- Для production можно мигрировать на K8s

**5. Как работает миграция данных в Django?**
```bash
python manage.py makemigrations  # создает файлы миграций
python manage.py migrate         # применяет к БД
```
- Django ORM генерирует SQL из Python кода
- История изменений БД в файлах `migrations/`

**6. Что такое middleware в Django?**
- Промежуточный слой обработки запросов
- Например: `CorsMiddleware` добавляет CORS заголовки, `AuthenticationMiddleware` определяет пользователя

---

## 📖 Дополнительные ресурсы

### Документация
- [Django 5.0 docs](https://docs.djangoproject.com/en/5.0/)
- [DRF docs](https://www.django-rest-framework.org/)
- [React docs](https://react.dev/)
- [PostgreSQL tutorial](https://www.postgresqltutorial.com/)

### YouTube каналы (русский)
- [Django уроки](https://www.youtube.com/@selfedu_rus) - Selfedu
- [Web Dev - JS/React](https://www.youtube.com/@webdevSimplified) - Web Dev Simplified (eng)

### Интерактивные курсы
- [freeCodeCamp - Backend](https://www.freecodecamp.org/learn/back-end-development-and-apis/)
- [React official tutorial](https://react.dev/learn/tutorial-tic-tac-toe)

---

## ✅ Чек-лист перед защитой

- [ ] Могу объяснить разницу REST API и GraphQL
- [ ] Понимаю как работает JWT аутентификация
- [ ] Могу нарисовать схему архитектуры проекта
- [ ] Знаю зачем нужен Docker Compose
- [ ] Понимаю разницу ORM vs SQL
- [ ] Могу объяснить зачем Celery + Redis
- [ ] Знаю основные HTTP методы (GET, POST, PUT, DELETE, PATCH)
- [ ] Понимаю CORS и почему он важен
- [ ] Могу объяснить Virtual DOM в React
- [ ] Знаю что такое middleware в Django

---

## 🎯 Шпаргалка "на коленке"

### 3 главных компонента бэкенда:
1. **Django ORM** - работа с БД через Python объекты
2. **DRF** - REST API endpoints
3. **Allauth + JWT** - аутентификация

### 3 главных компонента фронтенда:
1. **React** - UI компоненты
2. **Vite** - сборка и dev сервер
3. **Tailwind + Radix** - стили и UI библиотека

### 3 главных DevOps инструмента:
1. **Docker** - контейнеризация
2. **PostgreSQL** - реляционная БД
3. **Nginx** - reverse proxy

---

**Удачи на защите! 🚀**
