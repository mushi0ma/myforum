# Быстрый Старт

## 🚀 Запуск за 3 шага

### 1. Убедитесь что зависимости установлены
```bash
npm install
```
*(уже выполнено)*

### 2. Запустите dev-сервер
```bash
npm run dev
```

Откроется браузер на http://localhost:3000

### 3. Начните разработку!

## 📁 Где что находится

### Страницы
`src/pages/` - Здесь ваши страницы
- `Login.tsx` - Страница входа (главная `/`)
- `Register.tsx` - Страница регистрации (`/register`)

### Компоненты
`src/components/ui/` - 47 готовых UI компонентов
- Buttons, Inputs, Dialogs, Dropdowns и т.д.
- Все на основе Radix UI

### Стили
`src/index.css` - Глобальные стили и CSS-переменные
- Темная тема
- Шрифты
- CSS-переменные для цветов

### Ассеты
- `src/assets/` - Изображения и SVG (логотипы GitHub, GitLab, Reddit и т.д.)
- `public/fonts/` - Шрифты (Reddit Sans Chocolate, Monaspace Xenon)

## 🎨 Добавление новых страниц

1. Создайте файл в `src/pages/YourPage.tsx`
2. Добавьте роут в `src/App.tsx`:
```tsx
<Route path="/your-page" element={<YourPage />} />
```

## 🧩 Использование UI компонентов

Все компоненты уже импортированы и готовы к использованию:

```tsx
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Dialog } from '@/components/ui/dialog'

function MyComponent() {
  return (
    <div>
      <Button>Click me</Button>
      <Input placeholder="Enter text" />
    </div>
  )
}
```

## 🔧 Полезные команды

```bash
# Разработка
npm run dev          # Запуск dev-сервера

# Сборка
npm run build        # Production сборка
npm run preview      # Предпросмотр production сборки

# Проверка кода
npm run lint         # ESLint проверка
```

## 💡 Советы

1. **Tailwind CSS 4** - используйте utility классы прямо в JSX
2. **TypeScript** - все типы настроены, используйте автодополнение
3. **React 19** - используйте новые возможности (Server Components пока не поддерживаются в Vite)
4. **Path aliases** - используйте `@/` вместо относительных путей

## 🎯 Что дальше?

- Посмотрите примеры в `src/pages/Login.tsx` и `Register.tsx`
- Изучите доступные компоненты в `src/components/ui/`
- Настройте цвета в `src/index.css` (CSS-переменные в `:root`)
- Добавьте свои страницы и функциональность

## 📚 Документация

- Подробнее в `SETUP.md`
- Итоги рефакторинга в корне проекта: `CLEANUP_SUMMARY.md`

---

**Приятной разработки!** 🎉
